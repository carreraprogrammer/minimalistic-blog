<%= render @user%>
<div class='bioContainer'>
  <h2> Bio </h2>
  <p> <%= @user.bio %> </p>
</div>
  <%= render partial:"shared/post", collection: @user.recent_posts%>
  <button class="seeAllPostsButton">See All Posts</button>